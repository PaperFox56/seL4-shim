A very simple C shim exporting symbols from the libseL4 library. 
Useful when creating bindings for a language that doesn't support importing C headers.

Note that my use case only targets amd64 (x86_64) architectures but feel free to request or provide patches for other ones.
